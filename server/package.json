{
  "name": "sitepluginsserver",
  "version": "0.0.1",
  "description": "The server that orchestrates the SitePluginStore library",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
